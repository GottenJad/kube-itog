apiVersion: apps/v1
kind: Deployment
metadata:
  name: info-app
  namespace: bessjxx
spec:
  selector:
    matchLabels:
      app: info-app
  template:
    metadata:
      labels:
        app: info-app
    spec:
      containers:
        - name: info-app
          image: bessjxx/info-app:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "32Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          envFrom:
            - configMapRef:
                name: info-conf